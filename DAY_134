# 3074. Apple Redistribution into Boxes

class Solution {
public:
    int minimumBoxes(vector<int>& apple, vector<int>& capacity) {
        long long total = 0;
        for (int x : apple) total += x;
        
        sort(capacity.begin(), capacity.end(), greater<int>());
        
        long long curr = 0;
        int cnt = 0;
        for (int c : capacity) {
            curr += c;
            cnt++;
            if (curr >= total) return cnt;
        }
        return cnt;
    }
};
